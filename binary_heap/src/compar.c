#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include "binary_heap.h"

#define SIZE 1000

/*int main(int argc, char const *argv[])
{
    int i;
    time_t t;
    binary_heap_t heap;

    srand((unsigned)time(&t));
    for (i = 0 ; i < SIZE ; i++) {
        printf("% d \n", rand() % SIZE);
    }

    return 0;
}*/
